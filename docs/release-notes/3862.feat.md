Support column-chunked `csc`-in-{class}`dask.array.Array` input to {func}`scanpy.pp.highly_variable_genes` for all flavors (`seurat`, `cell_ranger`, `seurat_v3`, `seurat_v3_paper`), both single-batch and batched {smaller}`joncarse`
